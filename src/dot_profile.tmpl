#!/usr/bin/env sh
#
# Loaded at user login.

# Variables intended to be read-only. Used for further configuration to avoid making scripts templates.
export __FINLEY_OS="{{ .chezmoi.os }}"
export __FINLEY_IS_EPHEMERAL="{{ .ephemeral }}"
export __FINLEY_IS_HEADLESS="{{ .headless }}"
export __FINLEY_WORKSTATION_USE="{{ .workstation_use }}"
